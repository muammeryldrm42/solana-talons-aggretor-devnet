[package]
name = "router-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Deterministic route evaluation helpers for Talons Aggregator."

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "1"
