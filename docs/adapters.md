# Adapters

The current repo ships with a demo quote engine.

When you are ready:
- replace `lib/demo-engine.ts`,
- add real Raydium quote fetching,
- add Orca Whirlpool quote fetching,
- normalize outputs to the shared `QuoteResult` shape.
